<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.vectorforge</groupId>
    <artifactId>vectorforge</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>VectorForge</name>
    <description>Java-first vector search system with CPU and planned GPU backends.</description>

    <modules>
        <module>vectorforge-api</module>
        <module>vectorforge-cpu</module>
        <module>vectorforge-native</module>
        <module>vectorforge-gpu</module>
        <module>vectorforge-benchmarks</module>
        <module>vectorforge-lucene</module>
        <module>vectorforge-demo</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
        <junit.version>5.12.2</junit.version>
        <jmh.version>1.37</jmh.version>
        <exec.maven.plugin.version>3.5.0</exec.maven.plugin.version>
        <vectorforge.cuda.enabled>false</vectorforge.cuda.enabled>
        <vectorforge.cuvs.enabled>false</vectorforge.cuvs.enabled>
        <vectorforge.native.enabled>false</vectorforge.native.enabled>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.5.2</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${exec.maven.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>3.6.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <properties>
                <vectorforge.native.enabled>true</vectorforge.native.enabled>
            </properties>
        </profile>
        <profile>
            <id>cuda</id>
            <properties>
                <vectorforge.native.enabled>true</vectorforge.native.enabled>
                <vectorforge.cuda.enabled>true</vectorforge.cuda.enabled>
            </properties>
        </profile>
        <profile>
            <id>cuvs</id>
            <properties>
                <vectorforge.native.enabled>true</vectorforge.native.enabled>
                <vectorforge.cuda.enabled>true</vectorforge.cuda.enabled>
                <vectorforge.cuvs.enabled>true</vectorforge.cuvs.enabled>
            </properties>
        </profile>
    </profiles>
</project>
